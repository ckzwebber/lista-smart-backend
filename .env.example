# Porta em que o servidor escuta
PORT=3003

# Origens permitidas no CORS (separadas por vírgula).
# Inclua a URL do app/frontend que consome a API.
# Se vazio, usa os defaults de desenvolvimento (localhost).
CORS_ORIGIN=https://listasmart.cakowebber.dev
